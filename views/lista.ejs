<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Lista de Estudiantes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/estilos.css">
</head>
<body class="container mt-4">
  <h2 class="text-primary mb-3">📋 Lista de estudiantes</h2>
  <table class="table table-bordered table-striped">
    <thead class="table-primary">
      <tr><th>ID</th><th>Nombre</th><th>Edad</th><th>Acciones</th></tr>
    </thead>
    <tbody>
      <% estudiantes.forEach(est => { %>
        <tr>
          <td><%= est.id %></td>
          <td><%= est.nombre %></td>
          <td><%= est.edad %></td>
          <td>
            <a class="btn btn-sm btn-warning" href="/editar/<%= est.id %>">✏️ Editar</a>
            <a class="btn btn-sm btn-danger" href="/eliminar/<%= est.id %>" onclick="return confirm('¿Seguro que quieres eliminar este estudiante?')">🗑 Eliminar</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <a href="/formulario" class="btn btn-primary mt-3">➕ Agregar nuevo</a>
  <a href="/" class="btn btn-secondary mt-3">⬅ Volver</a>
</body>
</html>
